model:
  name: Qwen/Qwen3-8B
  revision: main
  load_in_4bit: true
  quant_type: nf4
  double_quant: true
  compute_dtype: bfloat16
  attn_implementation: sdpa
  trust_remote_code: true
lora:
  adapter_path: outputs/adapters/stage1
  r: 32
  alpha: 64
  dropout: 0.05
  bias: none
  target_modules: [q_proj, k_proj, v_proj, o_proj, gate_proj, up_proj, down_proj]
training:
  train_file: data/processed/train_long.jsonl
  validation_file: data/processed/validation_long.jsonl
  output_dir: outputs/checkpoints/stage2
  adapter_output_dir: outputs/adapters/stage2
  max_seq_length: 8192
  per_device_train_batch_size: 1
  per_device_eval_batch_size: 1
  gradient_accumulation_steps: 32
  learning_rate: 2.0e-5
  num_train_epochs: 0.5
  warmup_ratio: 0.03
  weight_decay: 0.01
  max_grad_norm: 1.0
  optim: paged_adamw_8bit
  lr_scheduler_type: cosine
  bf16: true
  fp16: false
  gradient_checkpointing: true
  use_cache: false
  logging_steps: 5
  eval_steps: 100
  save_steps: 100
  save_total_limit: 3
  seed: 42
  report_to: none
